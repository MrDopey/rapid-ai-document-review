node_modules
dist
coverage
playwright-report
test-results
package-lock.json
*.sqlite
specs
.specify
.github
# Local-only agent worktrees/state (see .git/info/exclude) — never part of the repo, but a nested
# worktree under here is a full checkout of the whole repo, so an unscoped `prettier .` walks into
# it and reports on files that aren't actually part of this checkout.
.claude
